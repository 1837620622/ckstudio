# CK Studio 改造说明

本项目在保留原有内容（项目卡片、开源仓库、关于信息、联系方式）的基础上，参考 [activetheory.net](https://activetheory.net/) 的 WebGL 沉浸式视觉风格，加入全屏 3D 背景、跨 section 渐进式动画、自主创新加载页、商业化文案改写。

---

## Mac 版本（macOS Sonoma / Ventura / Monterey）

### 一、目录结构
```
ckstudio-main/
├── index.html              # 入口页面（改造后的文案 + 全屏 canvas + 新加载页）
├── style.css               # 主样式（保留原版 + 末尾追加 3D/loader 样式）
├── script.js               # 主逻辑（保留项目/开源/复制/平滑滚动/动效）
├── js/
│   └── three-scene.js      # 【新增】Three.js 3D 场景（5 段渐进动画）
├── _headers                # Cloudflare Pages 安全头
├── 改造说明.md             # 当前文件
└── 同步发布工作区/          # 同步到虚拟机/部署用的临时目录
```

### 二、本地预览（必备）

#### 方式 1：Python 内置服务器（最简单）
```bash
cd /Users/chuankangkk/Downloads/ckstudio-main
python3 -m http.server 8765
```
打开浏览器访问 `http://localhost:8765/?v=1`（加 `?v=` 是为了绕过缓存，每次改完代码可换数字）。

#### 方式 2：Node 简易服务器
```bash
cd /Users/chuankangkk/Downloads/ckstudio-main
npx http-server -p 8765 -c-1
```

#### 方式 3：VS Code Live Server 插件
右键 `index.html` → "Open with Live Server"。

### 三、3D 场景技术细节

| 元素 | 说明 |
|------|------|
| **画布** | `<canvas id="scene-canvas">` 固定铺满全屏（`position: fixed; z-index: 0`），位于 body 顶层，所有 section 在其上方 |
| **Three.js** | 通过 CDN 加载 0.149 版本（`https://cdn.jsdelivr.net/npm/three@0.149.0/build/three.min.js`） |
| **5 段渐进动画** | Hero（圆环+CK+粒子）→ Works（圆环偏移淡出）→ OpenSource（网络节点+连线）→ About（二十面体几何）→ Contact（脉冲环扩散） |
| **滚动驱动** | `window.scroll` 事件计算 `scrollProgress (0~1)`，再 lerp 平滑过渡到目标状态（lerp rate 0.06） |
| **场景隔离** | `pointer-events: none`，3D 不会拦截鼠标点击 |
| **光标修复** | `body { cursor: default }` 恢复系统光标，避免鼠标消失 |

### 四、修改文案

所有业务化文案在 `index.html` 中可直接编辑，定位如下：

| Section | 行号区间 | 内容 |
|---------|----------|------|
| Hero 标题+介绍 | ~108-112 | 商业介绍（17 个项目 / 271+ API / AI 工具等） |
| Works 介绍 | ~151-153 | 项目方向（视频/音乐/图床/接口平台/云游戏/量化监控） |
| OpenSource 介绍 | ~174-176 | 仓库统计（47 个仓库 / 上千 Star / MIT 协议） |
| About WHO | ~229-230 | 独立开发者定位 + 全栈工程与 AI 应用 |
| About WHAT | ~233-234 | 服务范围 + 交付标准 |
| Contact | ~262-263 | 24h 响应 / 48h 初步方案 |
| Footer 标语 | ~340 | "Full Stack · API · AI Engineering" |

### 五、修改 3D 行为

编辑 `js/three-scene.js`：

- **CONFIG**：调整 `ringRadius`（圆环大小）、`particleCount`（粒子数量，桌面 1800 / 移动 600）
- **`updateSceneTargets()` 函数**：5 个 `currentScene === N` 分支定义每段的目标状态（圆环透明度/缩放/偏移、粒子范围、网络透明度、二十面体透明度、脉冲环透明度、相机距离）
- **颜色**：`particleColors` 数组控制粒子配色（金/绿/青）

### 六、部署到 Cloudflare Pages

#### 直接拖拽部署
1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/) → Pages
2. "Create a project" → "Direct Upload" → 拖入整个 `ckstudio-main/` 文件夹
3. 项目名（如 `ck-studio-2026`）→ "Deploy site"
4. 几秒钟后获得 `*.ck-studio-2026.pages.dev` 域名

#### Git 部署（推荐，自动部署）
```bash
cd /Users/chuankangkk/Downloads/ckstudio-main
git init
git add .
git commit -m "feat: WebGL 3D + Active Theory style"
gh repo create ck-studio-2026 --public --source=. --remote=origin --push
# 然后在 Cloudflare Pages 选 "Connect to Git" → 选这个仓库
```

#### 自定义域名
1. Cloudflare Pages → 项目 → "Custom domains" → "Set up a custom domain"
2. 输入 `yourdomain.com`，按提示去域名 DNS 添加 CNAME 记录
3. 自动签发 SSL，几分钟生效

### 七、常见问题

**Q: 3D 场景不显示？**
A: 检查浏览器 WebGL 支持（[测试页](https://get.webgl.org/)）。Three.js 通过 CDN 加载，需联网。

**Q: 鼠标在 hero 区域消失？**
A: 已修复：`body { cursor: default }`。如还有问题，强制刷新 `Cmd+Shift+R`。

**Q: 加载页只显示 1 秒就消失？**
A: 正常设计，模拟启动仪式感。如需延长，编辑 `script.js` 的 `initLoader`，把 `setTimeout(..., 1500)` 改大。

**Q: 移动端 3D 卡顿？**
A: `detectDevice()` 自动判断 4 核以下设备或 768px 以下屏幕，把粒子数从 1800 降到 600。可手动调 `CONFIG.particleMobileCount`。

**Q: 部署后 CSP 拦截 Three.js？**
A: `_headers` 已放行 `cdn.jsdelivr.net`。如换 CDN 需同步更新。

---

## Windows 版本（Windows 10 / 11）

### 一、目录结构（同上）

### 二、本地预览

#### 方式 1：Python 内置服务器
```powershell
cd C:\Users\你的用户名\Downloads\ckstudio-main
python -m http.server 8765
```
访问 `http://localhost:8765/?v=1`

> 如果系统无 Python，可从 [python.org](https://www.python.org/downloads/) 下载安装，勾选 "Add to PATH"。

#### 方式 2：Node 简易服务器
```powershell
cd C:\Users\你的用户名\Downloads\ckstudio-main
npx http-server -p 8765 -c-1
```

### 三、3D 场景技术细节（同上）

### 四、修改文案（同上）

### 五、修改 3D 行为（同上）

### 六、部署到 Cloudflare Pages

#### 直接拖拽部署
1. 登录 Cloudflare Dashboard → Pages
2. "Create a project" → "Direct Upload" → 拖入整个 `ckstudio-main/` 文件夹
3. 项目名 → "Deploy site"

#### Git 部署
```powershell
cd C:\Users\你的用户名\Downloads\ckstudio-main
git init
git add .
git commit -m "feat: WebGL 3D + Active Theory style"
gh repo create ck-studio-2026 --public --source=. --remote=origin --push
```

> 如未安装 `gh` CLI：从 [cli.github.com](https://cli.github.com/) 下载，或在 GitHub 手动创建仓库后 `git remote add origin ...` + `git push -u origin main`。

#### 部署到 IIS（自建 Windows 服务器）
1. 安装 IIS（"控制面板" → "程序" → "启用或关闭 Windows 功能" → 勾选 "Internet Information Services"）
2. 把 `ckstudio-main/` 全部文件复制到 `C:\inetpub\wwwroot\ckstudio\`
3. 创建 `web.config`（在网站根目录）：
```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <staticContent>
      <remove fileExtension=".webp" />
      <remove fileExtension=".woff2" />
    </staticContent>
  </system.webServer>
</configuration>
```
4. 打开 IIS Manager → 添加网站 → 物理路径选 `C:\inetpub\wwwroot\ckstudio` → 端口 80 → 启动

### 七、常见问题（同上）

**Q: 部署后访问 403？**
A: 检查文件权限：右键 `ckstudio-main` → "属性" → "安全" → 添加 "Everyone" 用户，赋予"读取"权限。

**Q: Windows 防火墙阻止？**
A: 首次运行 `python -m http.server` 会弹窗询问，允许通过。

**Q: 中文乱码？**
A: 确保 `index.html` 第 2 行是 `<meta charset="UTF-8">`（已设置）。如仍乱码，用 VS Code 打开 → 右下角编码 → "Save with Encoding" → UTF-8。

---

## 核心文件改动清单

| 文件 | 改动 |
|------|------|
| `index.html` | 6 处文案改写为商业介绍；hero 内部 canvas 移到 body 顶层；新增 3D canvas 节点 |
| `style.css` | 末尾追加 3D 画布样式 + 加载页样式（loader-dots/loader-num/loader-meta/progress-fill 等）+ 鼠标光标修复 |
| `script.js` | 新增 `initThreeScene` / `initScrollNav` / `initReveal` / `initCountUp`；`initLoader` 重写（数字递增 + 点阵节奏） |
| `js/three-scene.js` | **【新增】** 完整 3D 场景（圆环 + 中心 CK + 1800 粒子 + 网络节点 + 二十面体 + 脉冲环），按滚动进度切换 5 段状态 |

## 作者信息

- 微信：`1837620622`（传康Kk）
- 邮箱：`2040168455@qq.com`
- 咸鱼 / B 站：`万能程序员`

> 改造后所有视觉与功能均与 Active Theory 风格同源，但文案、配色（保留原金银青三色）、布局、交互逻辑均为 CK Studio 自有设计。
